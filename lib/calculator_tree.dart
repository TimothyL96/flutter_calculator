class CalculatorTree {
	String node;
	CalculatorTree leftChild;
	CalculatorTree rightChild;

	CalculatorTree( node, leftChild, rightChild ) {
		this.node = node;
		this.leftChild = leftChild;
		this.rightChild = rightChild;
	}
}